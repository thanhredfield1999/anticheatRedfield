name: Build and test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Test and build
        run: ./gradlew clean test build --no-daemon --console=plain

      - name: Upload plugin JAR
        uses: actions/upload-artifact@v4
        with:
          name: hybrid-anticheat-${{ github.sha }}
          path: anticheat-plugin/build/libs/*.jar
          if-no-files-found: error
          retention-days: 14
